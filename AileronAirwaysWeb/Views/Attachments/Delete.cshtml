@model Attachment

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Attachment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>
    
    <form method="post" asp-action="Delete" asp-route-eventId="@ViewBag.EventId" asp-route-attachmentId="@Model.Id">
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index" asp-route-eventId="@ViewBag.EventId">Back to List</a>
    </form>
</div>
