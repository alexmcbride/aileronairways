@model Echelon.TimelineApi.TimelineEvent

@{
    ViewData["Title"] = "Delete Timeline Event";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this event?</h3>
<div>
    <h4>@Html.DisplayFor(model => model.Title)</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.EventDateTime)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EventDateTime)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Location)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Location)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-route-timelineId="@ViewBag.TimelineId" asp-route-eventId="@Model.Id">
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index" asp-route-timelineId="@ViewBag.TimelineId">Back to List</a>
    </form>
</div>
